# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'UB Finder' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod "AlamofireImage"
  
  pod "Parse"

  pod "MessageInputBar"
  
  pod "lottie-ios"

  # Pods for UB Finder

  target 'UB FinderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'UB FinderUITests' do
    # Pods for testing
  end

   # Add Parse pods and install
   
end
